#include <iostream>
//#include <bitset>

//struct MyStruct {
//    int x;      // 4 bytes
//    char y;     // 1 byte
//    int z;      // 4 bytes
//};

int main() {
    

    //MyStruct m { 3, 3, 3 };

    //char* ptr = (char*)(&m.x);

    //unsigned int* ptr2 = (unsigned int*)(&m.y);

    //std::cout << *ptr2 << std::endl;

    //for (size_t i = 0; i < 12; i++)
    //{
    //    std::cout << (int*)(ptr + i) << " " << std::bitset<8>(*(ptr + i)) << std::endl;
    //}

    return 0;
}