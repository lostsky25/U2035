#include <iostream>
#include <iomanip>

int main()
{
    /*const int h = 5;
    const int w = 10;

    int** arr = new int* [h];

    for (int i = 0; i < h; i++)
        arr[i] = new int[w];

    for (int i = 0; i < h; i++)
    {
        for (int j = 0; j < w; j++)
        {
            arr[i][j] = (i + 1) * (j + 1);
        }
    }

    for (int i = 0; i < h; i++)
    {
        for (int j = 0; j < w; j++)
        {
            std::cout << std::setw(2) << arr[i][j] << ' ';
        }
        std::cout << std::endl;
    }

    for (int i = 0; i < h; i++)
        delete[] arr[i];

    delete arr;*/

    /*const int h = 5;
    const int w = 10;

    int arr[h][w];

    for (int i = 0; i < h; i++)
    {
        for (int j = 0; j < w; j++)
        {
            arr[i][j] = (i + 1) * (j + 1);
        }
    }

    for (int i = 0; i < h; i++)
    {
        for (int j = 0; j < w; j++)
        {
            std::cout << std::setw(2) << arr[i][j] << ' ';
        }
        std::cout << std::endl;
    }*/

    //int arr[5] = { 1, 2, 3, 4, 5 };
    //int* arr = new int[5];

    //for (int i = 0; i < 5; i++)
    //    arr[i] = i;

    //for (int i = 0; i < 5; i++)
    //    std::cout << *(arr + i) << ' ';

    //delete[] arr;

    return 0;
}