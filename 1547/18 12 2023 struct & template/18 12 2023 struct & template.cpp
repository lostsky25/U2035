#include <iostream>
#include "Triangle.h"

//#define MAX(x, y) (((x) > (y)) ? (x) : (y))

//template <class T>
//void max(T x, T y) {
//    return (x > y) ? x : y;
//}

int main() {


    //int a = 10;

    //max(++a, 10);

    /*Triangle<float> triangle;
    triangle.init({ 10, 20 }, { 30, 40 }, { -20, -30 });

    std::cout << &triangle << std::endl;
    triangle.square();*/

    return 0;
}