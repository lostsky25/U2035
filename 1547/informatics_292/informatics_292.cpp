#include <iostream>

int main() {
    int a;
    int b;

    std::cin >> a >> b;

    // std::min(a, b)
    // std::max(a, b)
    
    std::cout << std::max(a, b);

    // condition ? true : false;
    //std::cout << (a > b) ? a : b;

    //if (a > b) {
    //    std::cout << a;
    //}
    //else {
    //    std::cout << b;
    //}

    return 0;
}